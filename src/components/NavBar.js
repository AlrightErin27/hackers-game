import React from "react";

function NavBar() {
  //Once logged in this component will display.
  // Will have the links to log out, view past scores & back to home.

  return <div>NavBar</div>;
}

export default NavBar;
